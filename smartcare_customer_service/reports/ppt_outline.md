# 答辩 PPT 提纲（16 页，4 人分工）

## 分工总览

| 成员 | 负责页数 | 核心职责 |
| --- | --- | --- |
| 成员 A | 第 1-5 页 | 选题背景、系统架构、知识库与 RAG |
| 成员 B | 第 6-9 页 | 意图识别、Agent 路由、Function Calling |
| 成员 C | 第 10-13 页 | Memory、Web 界面、功能演示 |
| 成员 D | 第 14-16 页 | 实验评估、消融实验、总结改进 |

---

## 第 1 页：标题页（成员 A）

项目名称：SmartCare 端到端智能客服系统  
选题：题目 8：端到端智能客服系统  
场景：电商耳机售后客服  
成员：成员 A、成员 B、成员 C、成员 D

---

## 第 2 页：选题背景与目标（成员 A）

- 客服系统需同时完成问答、查订单、建工单、转人工和情绪安抚
- 课程覆盖点：RAG、Prompt Engineering、Function Calling、Agent 路由、Memory、评估
- 项目目标：做一个可运行、可演示、可评估的电商耳机售后智能客服
- 无需 GPU，自建数据，本地稳定演示

---

## 第 3 页：用户场景（成员 A）

展示五类典型场景，用简短对话截图或流程图：

1. 查物流："订单号 2024060112345 到哪了？"
2. 退换货："XX 耳机左耳没声音，我想换货"
3. 产品问答："XX 耳机防水吗？能戴着游泳吗？"
4. 产品对比："XX 和 YY 有什么区别？"
5. 情绪投诉："你们这什么破服务，我要投诉！"

---

## 第 4 页：系统总体架构（成员 A）

架构图：Web 界面 → HTTP API → SmartCareBot → 意图识别 → RAG / 工具 / Memory → 回复

- 前端：三栏布局（演示场景 | 聊天窗口 | 路由/工具/引用）
- 后端：Python 标准库，核心模块 `SmartCareBot`
- 数据层：知识库 Markdown + 模拟订单 JSON + SQLite 持久化

---

## 第 5 页：知识库构建与 RAG 检索（成员 A）

- 7 个知识库文件：32 条 FAQ、3 款耳机说明、售后政策、物流政策、客服流程
- 文档切分：按标题 + 空行分段，长段落滑动窗口
- TF-IDF 检索：中文单字/二元词/三元词 + 英文数字 token → 余弦相似度
- Top-3 检索 → 拼接 prompt → 带引用来源的回答
- 展示"XX 防水吗"的检索结果与来源截图

---

## 第 6 页：意图识别设计（成员 B）

- 八类意图：订单查询、退换货、产品咨询、保修、投诉、闲聊、转人工、兜底
- 槽位抽取：订单号（正则）、售后动作（退货/退款/换货/维修）、产品名（别名归一化）
- 规则方法：稳定可控、可解释、适合课程演示和答辩追问
- 改进方向：LLM 结构化输出、多意图拆解

---

## 第 7 页：Agent 路由决策（成员 B）

路由表/流程图：

- 订单查询 → `query_order`
- 退换货 → `create_ticket`
- 产品咨询 / 保修 / 闲聊 → RAG 知识库
- 投诉 / 人工 → `transfer_human`（高优先级）
- 低置信度 → 引导补充信息或转人工
- 强调：不是所有问题都交给生成模型，而是按场景选择路径

---

## 第 8 页：Function Calling 工具（成员 B）

四个工具：

| 工具 | 功能 | 触发场景 |
| --- | --- | --- |
| `query_order` | 查询订单物流 | 订单查询 |
| `create_ticket` | 创建售后工单 | 退换货 |
| `transfer_human` | 转人工客服 | 投诉/主动要求 |
| `compare_products` | 对比产品参数 | 产品咨询 |

放一个工具调用 JSON 示例（如 `create_ticket` 的请求和响应）

---

## 第 9 页：情绪识别与兜底策略（成员 B）

- 检测关键词："投诉、破服务、垃圾、气死、再不处理"
- angry 情绪 → 道歉共情 → `transfer_human`（priority=high）
- 知识库低置信度问题 → 不编造 → 说明无法确认 → 引导转人工
- 体现客服系统的安全与兜底设计

---

## 第 10 页：Memory 多轮记忆（成员 C）

- SQLite 三张表：`messages`（历史消息）、`session_state`（会话上下文）、`tickets`（工单）
- 会话状态字段：`last_order_id`、`last_product`、`pending_intent`、`last_issue`、`preferred_action`
- 实例：第 1 轮提供订单号 → 第 2 轮说"换一个吧" → 系统沿用上下文创建换货工单
- 不依赖 LLM 记忆能力，用结构化状态保证可靠性

---

## 第 11 页：Web 界面设计（成员 C）

- 三栏布局截图
  - 左侧：演示场景按钮 + 运行统计
  - 中间：客服聊天窗口 + 输入框
  - 右侧：本轮路由、工具调用 JSON、RAG 引用来源
- 设计理念：让老师同时看到"用户体验"和"系统内部决策"
- 技术：HTML/CSS/JavaScript 纯前端，无需框架

---

## 第 12 页：功能演示（成员 C）

现场演示 5 个场景（或播放录屏）：

1. 查物流 → 显示 `query_order` 返回结果
2. 知识库问答 → 显示 RAG 检索来源
3. 退换货 → 显示 `create_ticket` 工单创建
4. 产品对比 → 显示 `compare_products` 结果
5. 情绪投诉 → 显示高优先级 `transfer_human`

每步暂停展示右侧面板的路由和工具调用。

---

## 第 13 页：数据持久化与工程完整性（成员 C）

- SQLite 历史记录：系统重启后对话不丢失
- 会话状态持久化：多轮上下文跨轮次保持
- 工单记录：售后工单可追溯
- 项目结构规范：模块化代码、README、requirements.txt
- 满足课程指南中"产品完整性"加分项

---

## 第 14 页：20 条测试对话评估（成员 D）

- 测试集覆盖 9 个场景、20 条对话
- 运行 `python3 run_evaluation.py`
- 结果：
  - 意图识别准确率：100.0%
  - 工具调用正确率：100.0%
  - RAG 引用命中率：100.0%
- 解读：已知业务范围内功能完备，不代表开放场景泛化
- 展示评估结果表格

---

## 第 15 页：RAG Top-K 消融实验（成员 D）

- 8 个标注问题，比较 Top-1 / Top-3 / Top-5
- 运行 `python3 run_rag_ablation.py`
- 结果：
  - Top-1 命中率：87.5%
  - Top-3 命中率：100.0%
  - Top-5 命中率：100.0%
- 选择 Top-3 的理由：比 Top-1 更稳，比 Top-5 更精简
- 有 RAG vs 无 RAG 对比：RAG 使回答可追溯来源

---

## 第 16 页：总结与未来改进（成员 D）

项目完成度总结：

- ✅ Web 聊天界面
- ✅ RAG 知识库问答（带引用来源）
- ✅ 意图识别 + Agent 路由
- ✅ Function Calling（4 个工具）
- ✅ 多轮 Memory（SQLite 持久化）
- ✅ 情绪识别 + 安全兜底
- ✅ 20 条测试评估 + 消融实验

未来改进：

- 中文语义向量模型（BGE）+ ChromaDB 替换 TF-IDF
- Qwen2.5 JSON 模式做结构化意图识别
- 多意图拆解 + 真实客服语料
- 真实订单/工单接口 + 管理员后台
